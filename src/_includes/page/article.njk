---
layout: "/layouts/layout-article.njk"
---
<el-section id="article-page" padding-gap="10">
    <el-grid auto-fit="columns" columns="1" gap="2" pixel-width="768">

        <el-column attr="row">

            <div id="article-bio">
                <div class="article-author">
                    <img class="author-avatar" src="{{ site.author.avatar}}" alt="{{ site.author.name}} Avatar">
                    <div class="author-meta">
                       <p> <b> {{ site.author.name }} </b> </p>
                       <p> <time datetime="{{ date | dateIso }}">  {{ date | dateReadable }}  </time> </p>
                    </div>
                </div>
                <ul class="article-keywords">
                    {% for item in keywords %}
                        <li><b>{{ item }}</b></li>
                    {% endfor %}
                </ul>
            </div>

            <article id="article">
                {% block content  %}
                    {{ content | safe }}
                {% endblock content %}
            </article>
        </el-column>


    </el-grid>
</el-section>