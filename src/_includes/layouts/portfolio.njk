{% include "parts/head.njk" %}
    <body>
        {% include "navigations/navigation-portfolio.njk" %}
        <header>
            <el-section id="about" padding-gap="10" background="var(--color-quaternary)" color=var(--color-primary)>
                <el-grid columns=1>
                    <el-column attr="row" text-align="center">
                        <el-inline max-width=30 center=auto>  
                             <h1>
                                PORTFOLIO
                            </h1>
                        </el-inline>  
                    </el-column>
                </el-grid>
            </el-section>
        </header>
        <main>
            {{ content | safe }}
        </main>
        {% include "parts/footer.njk" %}
    </body>
    {% include "scripts/scriptsTags.njk" %}
</html>
